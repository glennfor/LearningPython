#python program to carry out some string operations
#Examples are: string reverse, string conactenation
import string
def str_rev(x):
    x=reversed(x)
    return x

g="reign"
str_rev(g)
print(str_rev(g))
